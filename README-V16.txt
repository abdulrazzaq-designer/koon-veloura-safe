Koon Veloura Home Tabs V16

Fixes:
- RTL/LTR separators appear only between tabs, never on an outside edge.
- Icon/name spacing increased and active pulse is clearer.
- Mobile active item is no longer clipped and uses less internal padding.
- Mobile active indicator is a Windows-style bottom bar attached to the menu edge.
- Neutral mobile glass remains a single flat layer.

Extract this ZIP into the project root, then run VERIFY-V16.ps1, pnpm production, and salla theme preview.
