V15 fixes
=========

- Keeps only one tab active-color setting.
- Shows selected tab icons and colors them with the tab text.
- Shows the icon selector only when Add Icon is enabled.
- Uses a fixed neutral glass color; no glass color setting.
- Fixes the mobile floating menu glass, active tile radius/size, and contained dot.
- Lowers the settings-title switch z-index so it does not cover Salla save controls.
- Preserves V14 header/tabs structure, sticky behavior, radii, solid-mode independent colors, and glass-mode unified surface.
