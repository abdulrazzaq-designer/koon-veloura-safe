Veloura V20 focused mobile floating menu patch

Fixes:
- active scale dropdown now changes the active tile and its content;
- menu color, text color, active color, icon size, font size and radius remain connected to settings;
- glass uses a uniform 44% alpha surface with the same blur strength as the header;
- active background follows the selected menu radius;
- active padding is compact and the tile rises only 5px;
- lower marker is visible and attached to the inner bottom edge;
- the soft lower gradient is restored without box-shadow.
