Veloura V19 focused mobile floating-menu fix

- Keeps the active item in a full-height slot, so its icon is not clipped.
- Draws the active background as an inset layer.
- Attaches the bottom indicator to the menu bottom edge.
- Restores the soft gradient below the floating menu.
- Does not modify tabs, header, twilight settings, or JavaScript.
