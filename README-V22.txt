V22 focuses on the mobile floating menu and the matching page-color fades only.

Changes:
- One active background layer only.
- Active tile uses a bounded radius and compact padding.
- 20x4 bottom indicator attached to the menu edge.
- Light-mode fade uses the configured site background.
- Dark-mode fade uses the configured dark body background.
- The same page-color fade is added behind the header/tabs stack.
- No JavaScript changes.
